import * as React from 'react';
import { Link } from 'react-router-dom';

function Wait() {
    return (
      <>
        <Header title={"WAIT"} />
        <OrderTable />
      </ >
    );
  }
  
  export default Wait;